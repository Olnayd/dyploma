
#include "CSqlConstants.h"

namespace database
{
    namespace constant
    {
        const common::version ImageStorageVersion(1, 1);
        const common::version TextStorageVersion(1, 1);
    }
}
